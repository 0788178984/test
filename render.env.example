# Copy these into Render → Environment (Dashboard). Do not commit real passwords to GitHub.

DATABASE_URL=postgresql://postgres.xmilhrvlmwkcphtheanx:YOUR_PASSWORD@aws-0-eu-west-1.pooler.supabase.com:5432/postgres?sslmode=require&uselibpqcompat=true
PGSSL_REJECT_UNAUTHORIZED=0
JWT_SECRET=use-a-long-random-string-here
NODE_ENV=production
TRUST_PROXY=1

# After client build — your public Render URL (no trailing slash)
# VITE_API_URL=https://your-service.onrender.com

# Supabase only — do not set DB_PATH or DB_BACKEND=sqlite on Render.
